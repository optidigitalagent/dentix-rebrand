# Browser-safe public booking configuration only.
# Keep booking disabled until the production backend and release checklist are complete.
VITE_DENTIX_BOOKING_API_URL=
VITE_DENTIX_BOOKING_ENABLED=false

# Public Google Apps Script Web App JSON endpoint. This is not a credential.
VITE_DENTIX_CONTENT_API_URL=
VITE_DENTIX_CONTENT_TIMEOUT_MS=4000
